# Testing

IMPORTANT: The testing requires ensight-reader (https://github.com/tkarabela/ensight-reader) which is only available from pip. The easiest way to get this in a conda env is then to navigate to the environment folder (e.g. shown by conda ls) and then pip install it directly into that folder 

Note that I keep a copy of this directory in the work directory but it needs to be moved to the directory from which 
you are running codes/submitting jobs from. It is only in this directory so it can be version controlled 
